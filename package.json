{
  "name": "tripweb-project",
  "version": "1.0.0",
  "description": "withTrip matching site",
  "scripts": {
    "build": "turbo run build",
  },
  "devDependencies": {
    "@types/passport-jwt": "^4.0.1",
    "turbo": "^2.0.7"
  }
}
